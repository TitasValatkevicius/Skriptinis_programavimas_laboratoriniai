%
% Paprastas skriptas
%

x = 1:32
y = x.^2

plot(x, y, 'o-r', x, y/3, 'xb')
title('Dvi funkcijos')
xlabel('X-ai')
ylabel('F_1 [-0-]    |   F_2 [-x-]')









N = 8

vektorius = N+1:0.5:N+4

A = N:N+8
A = reshape(A, 3, 3)'

A(3,2)

A(2:3, 1:2)

A([1, end], [1, end])

V = reshape(vektorius, 3, 3)'

%perda